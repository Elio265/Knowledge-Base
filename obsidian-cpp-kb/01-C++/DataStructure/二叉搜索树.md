---
tags: [cpp, 二叉搜索树, BST]
created: 2026-06-24
updated: 2026-06-25
status: 已掌握
---

# 二叉搜索树

## 一句话理解
二叉搜索树是一种按节点值大小有序排列的二叉树，左子树所有节点值小于根节点，右子树所有节点值大于根节点，利用二分思想实现高效查找。

## 核心原理

### 定义与性质
二叉搜索树（Binary Search Tree，BST）又称二叉排序树，要么是一棵空树，要么满足以下性质：
- 若左子树不为空，则左子树上所有节点的值都小于根节点的值
- 若右子树不为空，则右子树上所有节点的值都大于根节点的值
- 左右子树也分别为二叉搜索树

中序遍历二叉搜索树可以得到一个有序序列。

### 查找
从根节点开始比较，待查找值比当前节点大则往右子树走，小则往左子树走，相等则返回。重复直到找到或走到空节点。

最优时间复杂度：O(log n)，树高 log₂N
最差时间复杂度：O(n)，树退化为链表时

### 插入
先按查找规则找到合适位置，然后将新节点链接到树中。如果树为空，新节点直接作为根节点。如果待插入值已存在，插入失败。

### 删除（三种情况）
1. **叶子节点**：直接删除，父节点对应指针置空
2. **只有一个子节点**：将子节点"托孤"给父节点，然后删除当前节点
3. **有两个子节点**：找右子树最小节点（或左子树最大节点）替换待删除节点的值，然后删除该替换节点。此方法将"有两个孩子"的情况转化为删除叶子或单孩子节点。

## 底层实现

### 节点定义
```cpp
template<class K>
struct BStreeNode {
    BStreeNode(const K& key)
        : _key(key), _left(nullptr), _right(nullptr) {}
    K _key;
    BStreeNode* _left;
    BStreeNode* _right;
};
```

### 查找实现
```cpp
Node* find(const K& key) {
    Node* cur = _root;
    while (cur) {
        if (cur->_key < key) cur = cur->_right;
        else if (cur->_key > key) cur = cur->_left;
        else return cur;
    }
    return nullptr;
}
```

### 插入实现
```cpp
bool insert(const K& key) {
    if (_root == nullptr) {
        _root = new Node(key);
        return true;
    }
    Node* cur = _root;
    Node* parent = nullptr;
    while (cur) {
        parent = cur;
        if (cur->_key < key) cur = cur->_right;
        else if (cur->_key > key) cur = cur->_left;
        else return false; // 已存在
    }
    Node* newnode = new Node(key);
    parent->_key > key ? parent->_left = newnode : parent->_right = newnode;
    return true;
}
```

### 删除实现
```cpp
bool erase(const K& key) {
    // 查找节点及其父节点
    Node* cur = _root;
    Node* parent = nullptr;
    while (cur) {
        if (cur->_key < key) { parent = cur; cur = cur->_right; }
        else if (cur->_key > key) { parent = cur; cur = cur->_left; }
        else break;
    }
    if (cur == nullptr) return false;

    // 1. 叶子节点
    if (cur->_left == nullptr && cur->_right == nullptr) {
        if (parent == nullptr) _root = nullptr;
        else if (parent->_left == cur) parent->_left = nullptr;
        else parent->_right = nullptr;
        delete cur;
    }
    // 2. 只有一个孩子
    else if (cur->_left == nullptr || cur->_right == nullptr) {
        Node* child = cur->_left ? cur->_left : cur->_right;
        if (parent == nullptr) _root = child;
        else if (parent->_left == cur) parent->_left = child;
        else parent->_right = child;
        delete cur;
    }
    // 3. 有两个孩子：找右子树最小节点替换
    else {
        Node* replace = cur->_right;
        Node* replaceParent = cur;
        while (replace->_left) {
            replaceParent = replace;
            replace = replace->_left;
        }
        cur->_key = replace->_key;
        if (replaceParent->_left == replace)
            replaceParent->_left = replace->_right;
        else
            replaceParent->_right = replace->_right;
        delete replace;
    }
    return true;
}
```

### K模型与KV模型
- **K模型**：只存储 key，用于判断 key 是否存在（如拼写检查）
- **KV模型**：存储键值对 \<Key, Value\>，通过 key 快速找到 value（如英汉词典、单词计数）

## 常见应用场景
- 数据库索引（早期实现）
- 编译器符号表
- 路由表查找
- 关联容器（set / map 的底层基础结构）
- 各种需要有序数据快速查找的场景

## 容易踩坑的地方
1. **退化为链表**：当插入数据为有序序列（升序或降序）时，BST 退化为单链表，查找效率降为 O(n)。这是 BST 最核心的缺陷，必须使用平衡树解决。
2. **重复元素处理**：BST 默认不允许重复 key，如果有重复元素需求，需要改为插入到左子树或右子树的约定策略。
3. **删除两个孩子的节点**：替换后要正确删除替死鬼节点，而不是删除原节点，否则会丢失子树。
4. **父指针更新**：在删除操作中，父节点的指向必须正确更新，否则会导致内存泄漏或树结构断裂。

## 面试高频问题
1. 二叉搜索树的查找、插入、删除时间复杂度？最优和最优情况分别是多少？
2. 二叉搜索树退化为链表的原因是什么？如何解决？
3. 实现二叉搜索树的删除操作，重点考察两个孩子的处理方式。
4. 二叉搜索树的中序遍历结果是什么性质？（升序序列）
5. 二叉搜索树和 AVL 树、红黑树的区别是什么？
6. 给定一个二叉搜索树，如何找第 K 大的节点？

## 关联知识
- [[AVL树]]
- [[红黑树]]
- [[二叉树]]
- [[哈希表]]
- [[位图—布隆过滤器]]
